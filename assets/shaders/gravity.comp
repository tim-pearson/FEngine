#version 430 core

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(binding = 0) buffer transforms{
mat4 trs[];
};

void main()
{
    mat4 translateMatrix = mat4(
        1.0, 0.0, 0.0, 0.0,   // First column (x-axis)
        0.0, 1.0, 0.0, 1.0,   // Second column (y-axis)
        0.0, 0.0, 1.0, 0.0,   // Third column (z-axis)
        0.0, 0.0, 0.0, 1.0    // Fourth column (translation)
    );
    trs[gl_WorkGroupID.x] =  translateMatrix * trs[gl_WorkGroupID.x]

}

